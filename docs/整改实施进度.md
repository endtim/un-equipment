# 项目问题整改实施进度

## 第一轮（P0）完成项
- [x] 鉴权改为 JWT，受保护接口统一 `Authorization: Bearer <token>`。
- [x] 登录、注册、后台用户创建改为 BCrypt 哈希存储。
- [x] 登录链路移除明文密码兼容分支，仅保留 BCrypt 校验。
- [x] 统一错误响应结构 `code/msg/data`，补齐 401/403 语义。
- [x] `application.yml` 改为环境变量注入，提供 `application-local.yml.example`。
- [x] 订单、充值审核、系统管理补资源级权限边界。
- [x] 价格字段统一为 `priceInternal/priceExternal`，保留旧字段兼容输出。

## 第二轮（P1）完成项
- [x] 后台页面按业务拆分为独立路由组件（保留 `/admin/*` 路径不变）。
- [x] 后台壳层样式统一：标题区、工具栏、表格区、弹窗区、分页区。
- [x] 请求层统一：401/403 回登录、写操作 800ms 防重复提交、统一错误提示。
- [x] 用户中心、管理后台、门户页补固定可见的退出登录入口。
- [x] 列表筛选状态保留（query 回填）：用户、仪器、公告、帮助、日志等页面。
- [x] 核心文档（README、整改进度）统一为 UTF-8 中文可读版本。
- [x] 用户端核心页面中文修复：门户壳层、首页、用户中心壳层、后台壳层、账户页、路由元信息。
- [x] 首页登录面板样式协调：用户入口按钮统一为同宽同高、统一间距与对齐规则。
- [x] 前端字典统一升级：订单/充值/角色/仪器状态/开放模式等映射集中到 `frontend/src/utils/dicts.js`。
- [x] 主链路页面中文清理：仪器列表、仪器详情、我的订单、后台首页、统计页、仪器管理、充值审核已完成 UTF-8 修复。
- [x] 路由元信息修复：`frontend/src/router/index.js` 的标题/描述/面包屑统一恢复中文可读。
- [x] 旧聚合页清理：删除不再被路由引用的旧后台聚合组件，避免乱码回流与重复维护。

## 第三轮（P2）当前进展
- [x] 订单状态机第一版落地：动作-状态-类型矩阵校验，拦截非法流转。
- [x] 新增“已预约时间段”接口和前端可视化展示，提交前冲突校验。
- [x] 开放规则新增结构化字段（weekDay/startTime/endTime/effective dates/step/max），兼容旧字段。
- [x] 状态机规则文档化（`docs/api/order-state-machine.md`）。
- [x] 开放规则前后端枚举口径进一步收敛（含 UI 枚举下拉与错误码细化）。

## 第四轮（P3/P4）当前进展
- [~] 分层重构：`order` 模块已完成 VO/Assembler 首批改造，其他模块待继续。
- [x] 错误码字典化：`BizException` + 全局异常处理统一接入 `ErrorCodes`。
- [~] 日志审计：完成模块/动作中文映射，补齐 request method/uri/ip 链路字段。
- [x] 工程化收口：完善 `.gitignore`（IDE、构建产物、node_modules、日志、临时文件）。
- [~] 前端全站中文术语与枚举展示收敛（核心壳层与主路径已完成，剩余页面持续清理）。
- [x] README 再补一节“P2/P3 回归检查清单”（便于逐项自测打勾）。

## 新增文档
- `docs/api/error-codes.md`：统一错误码与模块语义。
- `docs/api/log-audit-view.md`：日志审计视图字段与展示规则。
- `docs/api/order-state-machine.md`：订单状态机动作矩阵与流转规则。

## 验证记录
- [x] 前端构建：`npm run build` 通过（存在体积告警，不影响功能）。
- [ ] 后端构建：受私有 Maven 仓库网络限制，当前环境未完成编译验证。

## 2026-03-11 补充推进
- [x] 价格口径一致化（按用户类型选价）已落地到：下单估价、上机完单重算、结算记录账单类型。
- [x] 结算并发幂等保护：`WAITING_SETTLEMENT -> SETTLING` 条件更新，避免重复扣费。
- [x] 开放规则输入约束收敛：`weekDay` 范围校验、`maxReserveMinutes/stepMinutes` 对齐校验。
- [x] 状态机文档同步：更新 `RESULT_UPLOADED`、`SETTLING`，并与当前取消状态口径保持一致。
- [x] README 已补充“P2/P3 回归检查清单”。
