# 项目问题整改实施进度

## 第一轮（P0）完成项
- [x] 鉴权改为 JWT，受保护接口统一 `Bearer Token`。
- [x] 登录、注册、后台用户创建改为 BCrypt 密码哈希。
- [x] 增加 401/403 标准语义，统一 `code/msg/data` 错误响应。
- [x] `application.yml` 改为环境变量注入，新增 `application-local.yml.example`。
- [x] 订单、充值审核、系统管理补充资源级权限边界。
- [x] 价格字段统一为 `priceInternal/priceExternal`，保留旧字段兼容输出。

## 第二轮（P1）完成项
- [x] 后台页面按业务拆分为独立路由组件（保持 `/admin/*` 路径不变）。
- [x] 重建后台壳层样式规范：标题区、工具栏、表格区、表单区统一。
- [x] 登录页、首页、后台页头导航、用户中心页头中文文案修复。
- [x] 列表筛选状态保留：用户、仪器、公告、帮助、日志页面支持 query 回填。
- [x] 请求层统一：401/403 回登录、写操作 800ms 防重复提交、统一异常提示。
- [x] 前后端校验对齐（重点字段）：用户名、密码、价格、开放规则、审核动作等。

## 待办（P2+）
- [ ] 订单状态机动作矩阵与非法流转统一约束。
- [ ] 开放规则结构化字段与统一枚举语义。
- [ ] DTO/VO/Assembler 分层重构与接口字段进一步收敛。
- [ ] 新人一键启动文档、SQL 初始化、默认账号说明补全。

## 验证记录
- [x] 前端构建：`npm run build` 通过。
- [ ] 后端构建：受私有 Maven 仓库网络限制，未在当前环境完成编译验证。
