<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unequipment.platform.modules.instrument.repository.TrainingQualificationRepository">

  <select id="countValidByUserAndInstrument" resultType="long">
    select count(1)
    from biz_instrument_training_qualification
    where deleted = 0
      and user_id = #{userId}
      and instrument_id = #{instrumentId}
      and status = 'PASSED'
      and (expire_time is null or expire_time &gt;= #{now})
  </select>

</mapper>

