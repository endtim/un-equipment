<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unequipment.platform.modules.finance.repository.TransactionRecordRepository">

  <select id="findByUserId" resultType="com.unequipment.platform.modules.finance.entity.TransactionRecord">
    select * from biz_transaction_record where user_id = #{userId} order by create_time desc
  </select>

  <select id="findPageByUserId" resultType="com.unequipment.platform.modules.finance.entity.TransactionRecord">
    <!-- 用户侧资金流水分页：按创建时间倒序 -->
    select *
    from biz_transaction_record
    where user_id = #{userId}
    order by create_time desc
    limit #{offset}, #{pageSize}
  </select>

  <select id="countByUserId" resultType="long">
    select count(1)
    from biz_transaction_record
    where user_id = #{userId}
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    insert into biz_transaction_record(
      txn_no, user_id, order_id, recharge_id, txn_type, inout_type, amount,
      balance_before, balance_after, remark, create_time
    ) values (
      #{txnNo}, #{userId}, #{orderId}, #{rechargeId}, #{txnType}, #{inoutType}, #{amount},
      #{balanceBefore}, #{balanceAfter}, #{remark}, #{createTime}
    )
  </insert>

</mapper>
